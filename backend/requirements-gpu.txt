# GPU 사용 시: 먼저 paddlepaddle (CPU) 는 설치하지 말고,
# 아래 중 CUDA 버전에 맞는 paddlepaddle-gpu 를 설치한 뒤
# pip install -r requirements-gpu.txt 실행.
#
# 예) CUDA 11.8:
#   pip install paddlepaddle-gpu==3.2.2 -i https://www.paddlepaddle.org.cn/packages/stable/cu118/
# 예) CUDA 12.6:
#   pip install paddlepaddle-gpu==3.2.2 -i https://www.paddlepaddle.org.cn/packages/stable/cu126/
# 예) CUDA 12.9:
#   pip install paddlepaddle-gpu==3.2.2 -i https://www.paddlepaddle.org.cn/packages/stable/cu129/
#
# paddlepaddle-gpu 는 위에서 따로 설치하므로 여기에는 포함하지 않음.

# FastAPI
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6

# Paddle OCR (paddlepaddle-gpu는 별도 설치)
paddleocr>=2.7.0

# PDF → 이미지 변환 (시스템에 Poppler 필요)
pdf2image>=1.16.3
pillow>=10.0.0
